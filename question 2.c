// program to mutiply two float numbers ?

#include <stdio.h>

int main()
{
 float a,b;
 scanf("%f %f",&a,&b);
 float mutiply = a*b;
 printf("the multiplication of a and b is %.2f",mutiply);
    return 0;
}
