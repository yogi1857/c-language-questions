//creating a c program to add two integers ? 

#include <stdio.h>

int main()
{
    int a,b;
    scanf(" %d %d",&a,&b);
    int sum = a+b ;
    printf(" the sum of a and b is %d",sum);

    return 0;
}
